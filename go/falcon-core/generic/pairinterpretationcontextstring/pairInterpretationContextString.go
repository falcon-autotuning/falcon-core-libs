// Code generated by go generate; DO NOT EDIT.
package pairinterpretationcontextstring

import (
	"errors"
	"runtime"
	"sync"
	"unsafe"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/autotuner-interfaces/interpretations/interpretationcontext"
	
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/errorhandling"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/str"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/utils"
)

/*
#cgo pkg-config: falcon_core_c_api
#include <falcon_core/generic/PairInterpretationContextString_c_api.h>
#include <falcon_core/generic/String_c_api.h>
#include <stdlib.h>
*/
import "C"

type chandle C.PairInterpretationContextStringHandle

type Handle struct {
	chandle      chandle
	mu           sync.RWMutex
	closed       bool
	errorHandler *errorhandling.Handle
}

func (h *Handle) CAPIHandle() (unsafe.Pointer, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("CAPIHandle The pair is closed")
	}
	return unsafe.Pointer(h.chandle), nil
}

func new(h chandle) *Handle {
	handle := &Handle{chandle: h, errorHandler: errorhandling.ErrorHandler}
	runtime.AddCleanup(handle, func(_ any) { handle.Close() }, true)
	return handle
}

func FromCAPI(p unsafe.Pointer) (*Handle, error) {
	if p == nil {
		return nil, errors.New("FromCAPI The pointer is null")
	}
	return new(chandle(p)), nil
}

func New(first *interpretationcontext.Handle, second string) (*Handle, error) {
  var (
    err error
    cFirst C.InterpretationContextHandle
    cSecond C.StringHandle
    h chandle
  )
  
    capiFirst, err := first.CAPIHandle()
    if err != nil {
      return nil, errors.Join(errors.New("failed to convert first to capi"), err)
    }
    cFirst = C.InterpretationContextHandle(capiFirst)
  
  
    strSecond := str.New(second)
    defer strSecond.Close()
    capiSecond, err := strSecond.CAPIHandle()
    if err != nil {
      return nil, errors.Join(errors.New("failed to convert first string to capi"), err)
    }
    cSecond = C.StringHandle(capiSecond)
  
	h = chandle(C.PairInterpretationContextString_create(cFirst, cSecond))
	err = errorhandling.ErrorHandler.CheckCapiError()
	if err != nil {
		return nil, err
	}
	return new(h), nil
}

func (h *Handle) Close() error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if !h.closed && h.chandle != utils.NilHandle[chandle]() {
		C.PairInterpretationContextString_destroy(C.PairInterpretationContextStringHandle(h.chandle))
		err := h.errorHandler.CheckCapiError()
		if err != nil {
			return err
		}
		h.closed = true
		h.chandle = utils.NilHandle[chandle]()
		return nil
	}
	return errors.New("unable to close the Pair")
}

func (h *Handle) First() (*interpretationcontext.Handle, error) {
	var err error
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		
		return nil, errors.New("First The pair is closed")
		
	}
	
	cHandle := C.PairInterpretationContextString_first(C.PairInterpretationContextStringHandle(h.chandle))
	val, err := interpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
	if err != nil {
		return nil, err
	}
	
	err = h.errorHandler.CheckCapiError()
	if err != nil {
		
		return nil, err
		
	}
	return val, nil
}

func (h *Handle) Second() (string, error) {
	var err error
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		
		return "", errors.New("Second The pair is closed")
		
	}
	
	strHandle, err := str.FromCAPI(unsafe.Pointer(C.PairInterpretationContextString_second(C.PairInterpretationContextStringHandle(h.chandle))))
	if err != nil {
		return "", err
	}
	defer strHandle.Close()
	val, err := strHandle.ToGoString()
  if err != nil {
    return "", errors.Join(errors.New(`Could not create go string`), err)
  }
	
	err = h.errorHandler.CheckCapiError()
	if err != nil {
		
		return "", err
		
	}
	return val, nil
}

func (h *Handle) Equal(other *Handle) (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Equal The pair is closed")
	}
	if other == nil {
		return false, errors.New("Equal The other pair is null")
	}
	other.mu.RLock()
	defer other.mu.RUnlock()
	if other.closed || other.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Equal The other pair is closed")
	}
	val := bool(C.PairInterpretationContextString_equal(C.PairInterpretationContextStringHandle(h.chandle), C.PairInterpretationContextStringHandle(other.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) NotEqual(other *Handle) (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("NotEqual The pair is closed")
	}
	if other == nil {
		return false, errors.New("NotEqual The other pair is null")
	}
	other.mu.RLock()
	defer other.mu.RUnlock()
	if other.closed || other.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("NotEqual The other pair is closed")
	}
	val := bool(C.PairInterpretationContextString_not_equal(C.PairInterpretationContextStringHandle(h.chandle), C.PairInterpretationContextStringHandle(other.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) ToJSON() (string, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return "", errors.New("ToJSON The pair is closed")
	}
	strHandle, err := str.FromCAPI(unsafe.Pointer(C.PairInterpretationContextString_to_json_string(C.PairInterpretationContextStringHandle(h.chandle))))
	if err != nil {
		return "", errors.New("ToJSON could not convert to a String. " + err.Error())
	}
	capiErr := h.errorHandler.CheckCapiError()
	if capiErr != nil {
		return "", capiErr
	}
	defer strHandle.Close()
	return strHandle.ToGoString()
}

func FromJSON(json string) (*Handle, error) {
	realJSON := str.New(json)
	defer realJSON.Close()
	capistr, err := realJSON.CAPIHandle()
	if err != nil {
		return nil, errors.Join(errors.New("failed to access capi for json"), err)
	}
	h := chandle(C.PairInterpretationContextString_from_json_string(C.StringHandle(capistr)))
	err = errorhandling.ErrorHandler.CheckCapiError()
	if err != nil {
		return nil, err
	}
	return new(h), nil
}
