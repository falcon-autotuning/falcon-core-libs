// Code generated by go generate; DO NOT EDIT.
package {{.Package}}

import (
	"errors"
	"runtime"
	"sync"
	"unsafe"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/errorhandling"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/{{.MapGoType}}"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/listinterpretationcontext"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/{{.ListValueGoType}}"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/{{.ListPairGoType}}"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/autotuner-interfaces/interpretations/interpretationcontext"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/listconnection"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/physics/deviceStructures/connections"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/physics/deviceStructures/connection"
  "github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/physics/units/symbolunit"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/generic/str"
	"github.com/falcon-autotuning/falcon-core-libs/go/falcon-core/utils"
	{{.OptionalImport}}
)

/*
#cgo pkg-config: falcon_core_c_api
#include <falcon_core/autotuner_interfaces/interpretations/{{.Header}}>
#include <falcon_core/generic/String_c_api.h>
#include <stdlib.h>
*/
import "C"

type chandle C.InterpretationContainer{{.ValueType}}Handle

type Handle struct {
	chandle      chandle
	mu           sync.RWMutex
	closed       bool
	errorHandler *errorhandling.Handle
}

func (h *Handle) CAPIHandle() (unsafe.Pointer, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("CAPIHandle: container is closed")
	}
	return unsafe.Pointer(h.chandle), nil
}

func new(h chandle) *Handle {
	handle := &Handle{chandle: h, errorHandler: errorhandling.ErrorHandler}
	runtime.SetFinalizer(handle, func(_ any) { handle.Close() })
	return handle
}

func FromCAPI(p unsafe.Pointer) (*Handle, error) {
	if p == nil {
		return nil, errors.New("FromCAPI: pointer is nil")
	}
	return new(chandle(p)), nil
}

func New(mapHandle *{{.MapGoType}}.Handle) (*Handle, error) {
	capi, err := mapHandle.CAPIHandle()
	if err != nil {
		return nil, err
	}
	h := chandle(C.InterpretationContainer{{.ValueType}}_create((C.MapInterpretationContext{{.ValueType}}Handle)(capi)))
	err = errorhandling.ErrorHandler.CheckCapiError()
	if err != nil {
		return nil, err
	}
	return new(h), nil
}

func (h *Handle) Close() error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if !h.closed && h.chandle != utils.NilHandle[chandle]() {
		C.InterpretationContainer{{.ValueType}}_destroy(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
		err := h.errorHandler.CheckCapiError()
		if err != nil {
			return err
		}
		h.closed = true
		h.chandle = utils.NilHandle[chandle]()
		return nil
	}
	return errors.New("unable to close the Handle")
}

func (h *Handle) Unit() (*symbolunit.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("Unit: container is closed")
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_unit(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
	return symbolunit.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) SelectByConnection(connection *connection.Handle) (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("SelectByConnection: container is closed")
	}
	capi, err := connection.CAPIHandle()
	if err != nil {
		return nil, err
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_select_by_connection(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.ConnectionHandle)(capi))
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) SelectByConnections(connections *connections.Handle) (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("SelectByConnections: container is closed")
	}
	capi, err := connections.CAPIHandle()
	if err != nil {
		return nil, err
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_select_by_connections(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.ConnectionsHandle)(capi))
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) SelectByIndependentConnection(connection *connection.Handle) (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("SelectByIndependentConnection: container is closed")
	}
	capi, err := connection.CAPIHandle()
	if err != nil {
		return nil, err
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_select_by_independent_connection(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.ConnectionHandle)(capi))
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) SelectByDependentConnection(connection *connection.Handle) (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("SelectByDependentConnection: container is closed")
	}
	capi, err := connection.CAPIHandle()
	if err != nil {
		return nil, err
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_select_by_dependent_connection(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.ConnectionHandle)(capi))
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) SelectContexts(independent *listconnection.Handle, dependent *listconnection.Handle) (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("SelectContexts: container is closed")
	}
	indepCapi, err := independent.CAPIHandle()
	if err != nil {
		return nil, err
	}
	depCapi, err := dependent.CAPIHandle()
	if err != nil {
		return nil, err
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_select_contexts(
		C.InterpretationContainer{{.ValueType}}Handle(h.chandle),
		(C.ListConnectionHandle)(indepCapi),
		(C.ListConnectionHandle)(depCapi),
	)
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) InsertOrAssign(key *interpretationcontext.Handle, value {{.GoValueType}}) error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return errors.New("InsertOrAssign: container is closed")
	}
	keyCapi, err := key.CAPIHandle()
	if err != nil {
		return err
	}
	{{if .ElemIsPrimitive}}
	C.InterpretationContainer{{.ValueType}}_insert_or_assign(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.{{.CValueType}})(value))
	{{else if .ElemIsString}}
	strVal := str.New(value)
	defer strVal.Close()
	valCapi, err := strVal.CAPIHandle()
	if err != nil {
		return err
	}
	C.InterpretationContainer{{.ValueType}}_insert_or_assign(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.StringHandle)(valCapi))
	{{else}}
	valCapi, err := value.CAPIHandle()
	if err != nil {
		return err
	}
	C.InterpretationContainer{{.ValueType}}_insert_or_assign(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.{{.CValueType}}Handle)(valCapi))
	{{end}}
	return h.errorHandler.CheckCapiError()
}

func (h *Handle) Insert(key *interpretationcontext.Handle, value {{.GoValueType}}) error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return errors.New("Insert: container is closed")
	}
	keyCapi, err := key.CAPIHandle()
	if err != nil {
		return err
	}
	{{if .ElemIsPrimitive}}
	C.InterpretationContainer{{.ValueType}}_insert(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.{{.CValueType}})(value))
	{{else if .ElemIsString}}
	strVal := str.New(value)
	defer strVal.Close()
	valCapi, err := strVal.CAPIHandle()
	if err != nil {
		return err
	}
	C.InterpretationContainer{{.ValueType}}_insert(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.StringHandle)(valCapi))
	{{else}}
	valCapi, err := value.CAPIHandle()
	if err != nil {
		return err
	}
	C.InterpretationContainer{{.ValueType}}_insert(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi), (C.{{.CValueType}}Handle)(valCapi))
	{{end}}
	return h.errorHandler.CheckCapiError()
}

func (h *Handle) At(key *interpretationcontext.Handle) ({{.GoValueType}}, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		{{if .ElemIsPrimitive}}
		return {{.GoValueType}}(0), errors.New("At: container is closed")
		{{else if .ElemIsString}}
		return "", errors.New("At: container is closed")
		{{else}}
		return nil, errors.New("At: container is closed")
		{{end}}
	}
	keyCapi, err := key.CAPIHandle()
	if err != nil {
		{{if .ElemIsPrimitive}}
		return {{.GoValueType}}(0), err
		{{else if .ElemIsString}}
		return "", err
		{{else}}
		return nil, err
		{{end}}
	}
	{{if .ElemIsPrimitive}}
	val := {{.GoValueType}}(C.InterpretationContainer{{.ValueType}}_at(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi)))
	{{else if .ElemIsString}}
	strHandle, err := str.FromCAPI(unsafe.Pointer(C.InterpretationContainer{{.ValueType}}_at(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi))))
	if err != nil {
		return "", err
	}
	defer strHandle.Close()
	val, err := strHandle.ToGoString()
	if err != nil {
		return "", err
	}
	{{else}}
	cHandle := C.InterpretationContainer{{.ValueType}}_at(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi))
	val, err := {{.GoValueConstructor}}(unsafe.Pointer(cHandle))
	if err != nil {
		return nil, err
	}
	{{end}}
	err = h.errorHandler.CheckCapiError()
	if err != nil {
		{{if .ElemIsPrimitive}}
		return {{.GoValueType}}(0), err
		{{else if .ElemIsString}}
		return "", err
		{{else}}
		return nil, err
		{{end}}
	}
	return val, nil
}

func (h *Handle) Erase(key *interpretationcontext.Handle) error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return errors.New("Erase: container is closed")
	}
	keyCapi, err := key.CAPIHandle()
	if err != nil {
		return err
	}
	C.InterpretationContainer{{.ValueType}}_erase(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi))
	return h.errorHandler.CheckCapiError()
}

func (h *Handle) Size() (int, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return 0, errors.New("Size: container is closed")
	}
	val := int(C.InterpretationContainer{{.ValueType}}_size(C.InterpretationContainer{{.ValueType}}Handle(h.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return 0, err
	}
	return val, nil
}

func (h *Handle) Empty() (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Empty: container is closed")
	}
	val := bool(C.InterpretationContainer{{.ValueType}}_empty(C.InterpretationContainer{{.ValueType}}Handle(h.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) Clear() error {
	h.mu.Lock()
	defer h.mu.Unlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return errors.New("Clear: container is closed")
	}
	C.InterpretationContainer{{.ValueType}}_clear(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
	return h.errorHandler.CheckCapiError()
}

func (h *Handle) Contains(key *interpretationcontext.Handle) (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Contains: container is closed")
	}
	keyCapi, err := key.CAPIHandle()
	if err != nil {
		return false, err
	}
	val := bool(C.InterpretationContainer{{.ValueType}}_contains(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), (C.InterpretationContextHandle)(keyCapi)))
	err = h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) Keys() (*listinterpretationcontext.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("Keys: container is closed")
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_keys(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
	return listinterpretationcontext.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) Values() (*{{.ListValueGoType}}.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("Values: container is closed")
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_values(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
	return {{.ListValueGoType}}.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) Items() (*{{.ListPairGoType}}.Handle, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return nil, errors.New("Items: container is closed")
	}
	cHandle := C.InterpretationContainer{{.ValueType}}_items(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))
	return {{.ListPairGoType}}.FromCAPI(unsafe.Pointer(cHandle))
}

func (h *Handle) Equal(other *Handle) (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Equal: container is closed")
	}
	if other == nil {
		return false, errors.New("Equal: other is nil")
	}
	other.mu.RLock()
	defer other.mu.RUnlock()
	if other.closed || other.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("Equal: other is closed")
	}
	val := bool(C.InterpretationContainer{{.ValueType}}_equal(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), C.InterpretationContainer{{.ValueType}}Handle(other.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) NotEqual(other *Handle) (bool, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("NotEqual: container is closed")
	}
	if other == nil {
		return false, errors.New("NotEqual: other is nil")
	}
	other.mu.RLock()
	defer other.mu.RUnlock()
	if other.closed || other.chandle == utils.NilHandle[chandle]() {
		return false, errors.New("NotEqual: other is closed")
	}
	val := bool(C.InterpretationContainer{{.ValueType}}_not_equal(C.InterpretationContainer{{.ValueType}}Handle(h.chandle), C.InterpretationContainer{{.ValueType}}Handle(other.chandle)))
	err := h.errorHandler.CheckCapiError()
	if err != nil {
		return false, err
	}
	return val, nil
}

func (h *Handle) ToJSON() (string, error) {
	h.mu.RLock()
	defer h.mu.RUnlock()
	if h.closed || h.chandle == utils.NilHandle[chandle]() {
		return "", errors.New("ToJSON: container is closed")
	}
	strHandle, err := str.FromCAPI(unsafe.Pointer(C.InterpretationContainer{{.ValueType}}_to_json_string(C.InterpretationContainer{{.ValueType}}Handle(h.chandle))))
	if err != nil {
		return "", err
	}
	defer strHandle.Close()
	return strHandle.ToGoString()
}

func FromJSON(json string) (*Handle, error) {
	realJSON := str.New(json)
	defer realJSON.Close()
	capistr, err := realJSON.CAPIHandle()
	if err != nil {
		return nil, err
	}
	h := chandle(C.InterpretationContainer{{.ValueType}}_from_json_string(C.StringHandle(capistr)))
	err = errorhandling.ErrorHandler.CheckCapiError()
	if err != nil {
		return nil, err
	}
	return new(h), nil
}
